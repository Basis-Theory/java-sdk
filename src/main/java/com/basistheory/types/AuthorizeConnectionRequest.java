/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.basistheory.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class AuthorizeConnectionRequest {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private AuthorizeConnectionRequest(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static AuthorizeConnectionRequest poll(AuthorizeConnectionRequestPoll value) {
        return new AuthorizeConnectionRequest(new PollValue(value));
    }

    public static AuthorizeConnectionRequest restart(AuthorizeConnectionRequestRestart value) {
        return new AuthorizeConnectionRequest(new RestartValue(value));
    }

    public static AuthorizeConnectionRequest present(AuthorizeConnectionRequestPresent value) {
        return new AuthorizeConnectionRequest(new PresentValue(value));
    }

    public boolean isPoll() {
        return value instanceof PollValue;
    }

    public boolean isRestart() {
        return value instanceof RestartValue;
    }

    public boolean isPresent() {
        return value instanceof PresentValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<AuthorizeConnectionRequestPoll> getPoll() {
        if (isPoll()) {
            return Optional.of(((PollValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AuthorizeConnectionRequestRestart> getRestart() {
        if (isRestart()) {
            return Optional.of(((RestartValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AuthorizeConnectionRequestPresent> getPresent() {
        if (isPresent()) {
            return Optional.of(((PresentValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AuthorizeConnectionRequest && value.equals(((AuthorizeConnectionRequest) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitPoll(AuthorizeConnectionRequestPoll poll);

        T visitRestart(AuthorizeConnectionRequestRestart restart);

        T visitPresent(AuthorizeConnectionRequestPresent present);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "action", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(PollValue.class),
        @JsonSubTypes.Type(RestartValue.class),
        @JsonSubTypes.Type(PresentValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("poll")
    @JsonIgnoreProperties("action")
    private static final class PollValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "action", allowSetters = true)
        private AuthorizeConnectionRequestPoll value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PollValue() {}

        private PollValue(AuthorizeConnectionRequestPoll value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPoll(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PollValue && equalTo((PollValue) other);
        }

        private boolean equalTo(PollValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AuthorizeConnectionRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("restart")
    @JsonIgnoreProperties("action")
    private static final class RestartValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "action", allowSetters = true)
        private AuthorizeConnectionRequestRestart value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RestartValue() {}

        private RestartValue(AuthorizeConnectionRequestRestart value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRestart(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RestartValue && equalTo((RestartValue) other);
        }

        private boolean equalTo(RestartValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AuthorizeConnectionRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("present")
    @JsonIgnoreProperties("action")
    private static final class PresentValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "action", allowSetters = true)
        private AuthorizeConnectionRequestPresent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PresentValue() {}

        private PresentValue(AuthorizeConnectionRequestPresent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPresent(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PresentValue && equalTo((PresentValue) other);
        }

        private boolean equalTo(PresentValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AuthorizeConnectionRequest{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("action")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AuthorizeConnectionRequest{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
