/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.basistheory.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class VerifyAllowanceRequest {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private VerifyAllowanceRequest(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static VerifyAllowanceRequest start(VerifyAllowanceRequestStart value) {
        return new VerifyAllowanceRequest(new StartValue(value));
    }

    public static VerifyAllowanceRequest submitSession(VerifyAllowanceRequestSubmitSession value) {
        return new VerifyAllowanceRequest(new SubmitSessionValue(value));
    }

    public static VerifyAllowanceRequest complete(VerifyAllowanceRequestComplete value) {
        return new VerifyAllowanceRequest(new CompleteValue(value));
    }

    public static VerifyAllowanceRequest selectOtpMethod(VerifyAllowanceRequestSelectOtpMethod value) {
        return new VerifyAllowanceRequest(new SelectOtpMethodValue(value));
    }

    public static VerifyAllowanceRequest submitOtp(VerifyAllowanceRequestSubmitOtp value) {
        return new VerifyAllowanceRequest(new SubmitOtpValue(value));
    }

    public static VerifyAllowanceRequest submitPasskey(VerifyAllowanceRequestSubmitPasskey value) {
        return new VerifyAllowanceRequest(new SubmitPasskeyValue(value));
    }

    public boolean isStart() {
        return value instanceof StartValue;
    }

    public boolean isSubmitSession() {
        return value instanceof SubmitSessionValue;
    }

    public boolean isComplete() {
        return value instanceof CompleteValue;
    }

    public boolean isSelectOtpMethod() {
        return value instanceof SelectOtpMethodValue;
    }

    public boolean isSubmitOtp() {
        return value instanceof SubmitOtpValue;
    }

    public boolean isSubmitPasskey() {
        return value instanceof SubmitPasskeyValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<VerifyAllowanceRequestStart> getStart() {
        if (isStart()) {
            return Optional.of(((StartValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<VerifyAllowanceRequestSubmitSession> getSubmitSession() {
        if (isSubmitSession()) {
            return Optional.of(((SubmitSessionValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<VerifyAllowanceRequestComplete> getComplete() {
        if (isComplete()) {
            return Optional.of(((CompleteValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<VerifyAllowanceRequestSelectOtpMethod> getSelectOtpMethod() {
        if (isSelectOtpMethod()) {
            return Optional.of(((SelectOtpMethodValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<VerifyAllowanceRequestSubmitOtp> getSubmitOtp() {
        if (isSubmitOtp()) {
            return Optional.of(((SubmitOtpValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<VerifyAllowanceRequestSubmitPasskey> getSubmitPasskey() {
        if (isSubmitPasskey()) {
            return Optional.of(((SubmitPasskeyValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof VerifyAllowanceRequest && value.equals(((VerifyAllowanceRequest) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitStart(VerifyAllowanceRequestStart start);

        T visitSubmitSession(VerifyAllowanceRequestSubmitSession submitSession);

        T visitComplete(VerifyAllowanceRequestComplete complete);

        T visitSelectOtpMethod(VerifyAllowanceRequestSelectOtpMethod selectOtpMethod);

        T visitSubmitOtp(VerifyAllowanceRequestSubmitOtp submitOtp);

        T visitSubmitPasskey(VerifyAllowanceRequestSubmitPasskey submitPasskey);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "action", visible = true, defaultImpl = StartValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(StartValue.class),
        @JsonSubTypes.Type(SubmitSessionValue.class),
        @JsonSubTypes.Type(CompleteValue.class),
        @JsonSubTypes.Type(SelectOtpMethodValue.class),
        @JsonSubTypes.Type(SubmitOtpValue.class),
        @JsonSubTypes.Type(SubmitPasskeyValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("start")
    @JsonIgnoreProperties("action")
    private static final class StartValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "action", allowSetters = true)
        private VerifyAllowanceRequestStart value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private StartValue() {}

        private StartValue(VerifyAllowanceRequestStart value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitStart(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof StartValue && equalTo((StartValue) other);
        }

        private boolean equalTo(StartValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "VerifyAllowanceRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("submit_session")
    @JsonIgnoreProperties("action")
    private static final class SubmitSessionValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "action", allowSetters = true)
        private VerifyAllowanceRequestSubmitSession value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SubmitSessionValue() {}

        private SubmitSessionValue(VerifyAllowanceRequestSubmitSession value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSubmitSession(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SubmitSessionValue && equalTo((SubmitSessionValue) other);
        }

        private boolean equalTo(SubmitSessionValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "VerifyAllowanceRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("complete")
    @JsonIgnoreProperties("action")
    private static final class CompleteValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "action", allowSetters = true)
        private VerifyAllowanceRequestComplete value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CompleteValue() {}

        private CompleteValue(VerifyAllowanceRequestComplete value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitComplete(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CompleteValue && equalTo((CompleteValue) other);
        }

        private boolean equalTo(CompleteValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "VerifyAllowanceRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("select_otp_method")
    @JsonIgnoreProperties("action")
    private static final class SelectOtpMethodValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "action", allowSetters = true)
        private VerifyAllowanceRequestSelectOtpMethod value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SelectOtpMethodValue() {}

        private SelectOtpMethodValue(VerifyAllowanceRequestSelectOtpMethod value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSelectOtpMethod(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SelectOtpMethodValue && equalTo((SelectOtpMethodValue) other);
        }

        private boolean equalTo(SelectOtpMethodValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "VerifyAllowanceRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("submit_otp")
    @JsonIgnoreProperties("action")
    private static final class SubmitOtpValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "action", allowSetters = true)
        private VerifyAllowanceRequestSubmitOtp value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SubmitOtpValue() {}

        private SubmitOtpValue(VerifyAllowanceRequestSubmitOtp value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSubmitOtp(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SubmitOtpValue && equalTo((SubmitOtpValue) other);
        }

        private boolean equalTo(SubmitOtpValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "VerifyAllowanceRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("submit_passkey")
    @JsonIgnoreProperties("action")
    private static final class SubmitPasskeyValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "action", allowSetters = true)
        private VerifyAllowanceRequestSubmitPasskey value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SubmitPasskeyValue() {}

        private SubmitPasskeyValue(VerifyAllowanceRequestSubmitPasskey value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSubmitPasskey(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SubmitPasskeyValue && equalTo((SubmitPasskeyValue) other);
        }

        private boolean equalTo(SubmitPasskeyValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "VerifyAllowanceRequest{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("action")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "VerifyAllowanceRequest{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
