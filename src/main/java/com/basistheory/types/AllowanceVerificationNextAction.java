/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.basistheory.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class AllowanceVerificationNextAction {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private AllowanceVerificationNextAction(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static AllowanceVerificationNextAction selectOtpMethod(
            AllowanceVerificationNextActionSelectOtpMethod value) {
        return new AllowanceVerificationNextAction(new SelectOtpMethodValue(value));
    }

    public static AllowanceVerificationNextAction otp(AllowanceVerificationNextActionOtp value) {
        return new AllowanceVerificationNextAction(new OtpValue(value));
    }

    public static AllowanceVerificationNextAction passkeySession(AllowanceVerificationNextActionPasskeySession value) {
        return new AllowanceVerificationNextAction(new PasskeySessionValue(value));
    }

    public static AllowanceVerificationNextAction passkey(AllowanceVerificationNextActionPasskey value) {
        return new AllowanceVerificationNextAction(new PasskeyValue(value));
    }

    public static AllowanceVerificationNextAction redirect(AllowanceVerificationNextActionRedirect value) {
        return new AllowanceVerificationNextAction(new RedirectValue(value));
    }

    public boolean isSelectOtpMethod() {
        return value instanceof SelectOtpMethodValue;
    }

    public boolean isOtp() {
        return value instanceof OtpValue;
    }

    public boolean isPasskeySession() {
        return value instanceof PasskeySessionValue;
    }

    public boolean isPasskey() {
        return value instanceof PasskeyValue;
    }

    public boolean isRedirect() {
        return value instanceof RedirectValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<AllowanceVerificationNextActionSelectOtpMethod> getSelectOtpMethod() {
        if (isSelectOtpMethod()) {
            return Optional.of(((SelectOtpMethodValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AllowanceVerificationNextActionOtp> getOtp() {
        if (isOtp()) {
            return Optional.of(((OtpValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AllowanceVerificationNextActionPasskeySession> getPasskeySession() {
        if (isPasskeySession()) {
            return Optional.of(((PasskeySessionValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AllowanceVerificationNextActionPasskey> getPasskey() {
        if (isPasskey()) {
            return Optional.of(((PasskeyValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AllowanceVerificationNextActionRedirect> getRedirect() {
        if (isRedirect()) {
            return Optional.of(((RedirectValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AllowanceVerificationNextAction
                && value.equals(((AllowanceVerificationNextAction) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitSelectOtpMethod(AllowanceVerificationNextActionSelectOtpMethod selectOtpMethod);

        T visitOtp(AllowanceVerificationNextActionOtp otp);

        T visitPasskeySession(AllowanceVerificationNextActionPasskeySession passkeySession);

        T visitPasskey(AllowanceVerificationNextActionPasskey passkey);

        T visitRedirect(AllowanceVerificationNextActionRedirect redirect);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(SelectOtpMethodValue.class),
        @JsonSubTypes.Type(OtpValue.class),
        @JsonSubTypes.Type(PasskeySessionValue.class),
        @JsonSubTypes.Type(PasskeyValue.class),
        @JsonSubTypes.Type(RedirectValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("select_otp_method")
    @JsonIgnoreProperties("type")
    private static final class SelectOtpMethodValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private AllowanceVerificationNextActionSelectOtpMethod value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SelectOtpMethodValue() {}

        private SelectOtpMethodValue(AllowanceVerificationNextActionSelectOtpMethod value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSelectOtpMethod(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SelectOtpMethodValue && equalTo((SelectOtpMethodValue) other);
        }

        private boolean equalTo(SelectOtpMethodValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AllowanceVerificationNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("otp")
    @JsonIgnoreProperties("type")
    private static final class OtpValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private AllowanceVerificationNextActionOtp value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private OtpValue() {}

        private OtpValue(AllowanceVerificationNextActionOtp value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitOtp(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof OtpValue && equalTo((OtpValue) other);
        }

        private boolean equalTo(OtpValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AllowanceVerificationNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("passkey_session")
    @JsonIgnoreProperties("type")
    private static final class PasskeySessionValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private AllowanceVerificationNextActionPasskeySession value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PasskeySessionValue() {}

        private PasskeySessionValue(AllowanceVerificationNextActionPasskeySession value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPasskeySession(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PasskeySessionValue && equalTo((PasskeySessionValue) other);
        }

        private boolean equalTo(PasskeySessionValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AllowanceVerificationNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("passkey")
    @JsonIgnoreProperties("type")
    private static final class PasskeyValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private AllowanceVerificationNextActionPasskey value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PasskeyValue() {}

        private PasskeyValue(AllowanceVerificationNextActionPasskey value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPasskey(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PasskeyValue && equalTo((PasskeyValue) other);
        }

        private boolean equalTo(PasskeyValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AllowanceVerificationNextAction{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("redirect")
    @JsonIgnoreProperties("type")
    private static final class RedirectValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private AllowanceVerificationNextActionRedirect value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RedirectValue() {}

        private RedirectValue(AllowanceVerificationNextActionRedirect value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRedirect(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RedirectValue && equalTo((RedirectValue) other);
        }

        private boolean equalTo(RedirectValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AllowanceVerificationNextAction{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AllowanceVerificationNextAction{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
