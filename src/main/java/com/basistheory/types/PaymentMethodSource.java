/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.basistheory.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class PaymentMethodSource {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private PaymentMethodSource(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static PaymentMethodSource basisTheoryCardToken(PaymentMethodSourceBasisTheoryCardToken value) {
        return new PaymentMethodSource(new BasisTheoryCardTokenValue(value));
    }

    public static PaymentMethodSource connection(PaymentMethodSourceConnection value) {
        return new PaymentMethodSource(new ConnectionValue(value));
    }

    public boolean isBasisTheoryCardToken() {
        return value instanceof BasisTheoryCardTokenValue;
    }

    public boolean isConnection() {
        return value instanceof ConnectionValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<PaymentMethodSourceBasisTheoryCardToken> getBasisTheoryCardToken() {
        if (isBasisTheoryCardToken()) {
            return Optional.of(((BasisTheoryCardTokenValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<PaymentMethodSourceConnection> getConnection() {
        if (isConnection()) {
            return Optional.of(((ConnectionValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PaymentMethodSource && value.equals(((PaymentMethodSource) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitBasisTheoryCardToken(PaymentMethodSourceBasisTheoryCardToken basisTheoryCardToken);

        T visitConnection(PaymentMethodSourceConnection connection);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(BasisTheoryCardTokenValue.class), @JsonSubTypes.Type(ConnectionValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("basis_theory_card_token")
    @JsonIgnoreProperties("type")
    private static final class BasisTheoryCardTokenValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private PaymentMethodSourceBasisTheoryCardToken value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BasisTheoryCardTokenValue() {}

        private BasisTheoryCardTokenValue(PaymentMethodSourceBasisTheoryCardToken value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBasisTheoryCardToken(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BasisTheoryCardTokenValue && equalTo((BasisTheoryCardTokenValue) other);
        }

        private boolean equalTo(BasisTheoryCardTokenValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentMethodSource{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("connection")
    @JsonIgnoreProperties("type")
    private static final class ConnectionValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "type", allowSetters = true)
        private PaymentMethodSourceConnection value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ConnectionValue() {}

        private ConnectionValue(PaymentMethodSourceConnection value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitConnection(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ConnectionValue && equalTo((ConnectionValue) other);
        }

        private boolean equalTo(ConnectionValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentMethodSource{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentMethodSource{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
