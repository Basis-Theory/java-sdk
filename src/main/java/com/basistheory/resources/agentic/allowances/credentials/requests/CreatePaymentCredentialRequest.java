/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.basistheory.resources.agentic.allowances.credentials.requests;

import com.basistheory.core.ObjectMappers;
import com.basistheory.resources.agentic.allowances.credentials.types.CreatePaymentCredentialRequestCredential;
import com.basistheory.resources.agentic.allowances.credentials.types.CreatePaymentCredentialRequestProvider;
import com.basistheory.resources.agentic.allowances.credentials.types.CreatePaymentCredentialRequestRail;
import com.basistheory.types.SharedPaymentAmount;
import com.basistheory.types.SharedPaymentMerchant;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreatePaymentCredentialRequest.Builder.class)
public final class CreatePaymentCredentialRequest {
    private final Optional<String> btIdempotencyKey;

    private final CreatePaymentCredentialRequestRail rail;

    private final CreatePaymentCredentialRequestProvider provider;

    private final Optional<SharedPaymentAmount> amount;

    private final Optional<SharedPaymentMerchant> merchant;

    private final CreatePaymentCredentialRequestCredential credential;

    private final Map<String, Object> additionalProperties;

    private CreatePaymentCredentialRequest(
            Optional<String> btIdempotencyKey,
            CreatePaymentCredentialRequestRail rail,
            CreatePaymentCredentialRequestProvider provider,
            Optional<SharedPaymentAmount> amount,
            Optional<SharedPaymentMerchant> merchant,
            CreatePaymentCredentialRequestCredential credential,
            Map<String, Object> additionalProperties) {
        this.btIdempotencyKey = btIdempotencyKey;
        this.rail = rail;
        this.provider = provider;
        this.amount = amount;
        this.merchant = merchant;
        this.credential = credential;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Optional stable key for detecting retries. A successful bearer credential cannot be replayed.
     */
    @JsonIgnore
    public Optional<String> getBtIdempotencyKey() {
        return btIdempotencyKey;
    }

    @JsonProperty("rail")
    public CreatePaymentCredentialRequestRail getRail() {
        return rail;
    }

    @JsonProperty("provider")
    public CreatePaymentCredentialRequestProvider getProvider() {
        return provider;
    }

    @JsonProperty("amount")
    public Optional<SharedPaymentAmount> getAmount() {
        return amount;
    }

    /**
     * @return Merchant this credential is being minted for. Required when the allowance has no <code>merchant</code>, and rejected when it does — the allowance's merchant is the scope the cardholder verified against, so a mint can neither restate nor replace it. Required on every rail for consistency. At mint Visa (<code>vic</code>) forwards it to the network, and Stripe Link (<code>link</code>) names it on the spend request the consumer sees.
     */
    @JsonProperty("merchant")
    public Optional<SharedPaymentMerchant> getMerchant() {
        return merchant;
    }

    @JsonProperty("credential")
    public CreatePaymentCredentialRequestCredential getCredential() {
        return credential;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreatePaymentCredentialRequest && equalTo((CreatePaymentCredentialRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreatePaymentCredentialRequest other) {
        return btIdempotencyKey.equals(other.btIdempotencyKey)
                && rail.equals(other.rail)
                && provider.equals(other.provider)
                && amount.equals(other.amount)
                && merchant.equals(other.merchant)
                && credential.equals(other.credential);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.btIdempotencyKey, this.rail, this.provider, this.amount, this.merchant, this.credential);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static RailStage builder() {
        return new Builder();
    }

    public interface RailStage {
        ProviderStage rail(@NotNull CreatePaymentCredentialRequestRail rail);

        Builder from(CreatePaymentCredentialRequest other);
    }

    public interface ProviderStage {
        CredentialStage provider(@NotNull CreatePaymentCredentialRequestProvider provider);
    }

    public interface CredentialStage {
        _FinalStage credential(@NotNull CreatePaymentCredentialRequestCredential credential);
    }

    public interface _FinalStage {
        CreatePaymentCredentialRequest build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>Optional stable key for detecting retries. A successful bearer credential cannot be replayed.</p>
         */
        _FinalStage btIdempotencyKey(Optional<String> btIdempotencyKey);

        _FinalStage btIdempotencyKey(String btIdempotencyKey);

        _FinalStage amount(Optional<SharedPaymentAmount> amount);

        _FinalStage amount(SharedPaymentAmount amount);

        /**
         * <p>Merchant this credential is being minted for. Required when the allowance has no <code>merchant</code>, and rejected when it does — the allowance's merchant is the scope the cardholder verified against, so a mint can neither restate nor replace it. Required on every rail for consistency. At mint Visa (<code>vic</code>) forwards it to the network, and Stripe Link (<code>link</code>) names it on the spend request the consumer sees.</p>
         */
        _FinalStage merchant(Optional<SharedPaymentMerchant> merchant);

        _FinalStage merchant(SharedPaymentMerchant merchant);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements RailStage, ProviderStage, CredentialStage, _FinalStage {
        private CreatePaymentCredentialRequestRail rail;

        private CreatePaymentCredentialRequestProvider provider;

        private CreatePaymentCredentialRequestCredential credential;

        private Optional<SharedPaymentMerchant> merchant = Optional.empty();

        private Optional<SharedPaymentAmount> amount = Optional.empty();

        private Optional<String> btIdempotencyKey = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreatePaymentCredentialRequest other) {
            btIdempotencyKey(other.getBtIdempotencyKey());
            rail(other.getRail());
            provider(other.getProvider());
            amount(other.getAmount());
            merchant(other.getMerchant());
            credential(other.getCredential());
            return this;
        }

        @java.lang.Override
        @JsonSetter("rail")
        public ProviderStage rail(@NotNull CreatePaymentCredentialRequestRail rail) {
            this.rail = Objects.requireNonNull(rail, "rail must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("provider")
        public CredentialStage provider(@NotNull CreatePaymentCredentialRequestProvider provider) {
            this.provider = Objects.requireNonNull(provider, "provider must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("credential")
        public _FinalStage credential(@NotNull CreatePaymentCredentialRequestCredential credential) {
            this.credential = Objects.requireNonNull(credential, "credential must not be null");
            return this;
        }

        /**
         * <p>Merchant this credential is being minted for. Required when the allowance has no <code>merchant</code>, and rejected when it does — the allowance's merchant is the scope the cardholder verified against, so a mint can neither restate nor replace it. Required on every rail for consistency. At mint Visa (<code>vic</code>) forwards it to the network, and Stripe Link (<code>link</code>) names it on the spend request the consumer sees.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage merchant(SharedPaymentMerchant merchant) {
            this.merchant = Optional.ofNullable(merchant);
            return this;
        }

        /**
         * <p>Merchant this credential is being minted for. Required when the allowance has no <code>merchant</code>, and rejected when it does — the allowance's merchant is the scope the cardholder verified against, so a mint can neither restate nor replace it. Required on every rail for consistency. At mint Visa (<code>vic</code>) forwards it to the network, and Stripe Link (<code>link</code>) names it on the spend request the consumer sees.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "merchant", nulls = Nulls.SKIP)
        public _FinalStage merchant(Optional<SharedPaymentMerchant> merchant) {
            this.merchant = merchant;
            return this;
        }

        @java.lang.Override
        public _FinalStage amount(SharedPaymentAmount amount) {
            this.amount = Optional.ofNullable(amount);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "amount", nulls = Nulls.SKIP)
        public _FinalStage amount(Optional<SharedPaymentAmount> amount) {
            this.amount = amount;
            return this;
        }

        /**
         * <p>Optional stable key for detecting retries. A successful bearer credential cannot be replayed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage btIdempotencyKey(String btIdempotencyKey) {
            this.btIdempotencyKey = Optional.ofNullable(btIdempotencyKey);
            return this;
        }

        /**
         * <p>Optional stable key for detecting retries. A successful bearer credential cannot be replayed.</p>
         */
        @java.lang.Override
        public _FinalStage btIdempotencyKey(Optional<String> btIdempotencyKey) {
            this.btIdempotencyKey = btIdempotencyKey;
            return this;
        }

        @java.lang.Override
        public CreatePaymentCredentialRequest build() {
            return new CreatePaymentCredentialRequest(
                    btIdempotencyKey, rail, provider, amount, merchant, credential, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
