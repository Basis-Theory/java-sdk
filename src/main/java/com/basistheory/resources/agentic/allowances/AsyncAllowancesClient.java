/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.basistheory.resources.agentic.allowances;

import com.basistheory.core.ClientOptions;
import com.basistheory.core.RequestOptions;
import com.basistheory.core.Suppliers;
import com.basistheory.core.pagination.SyncPagingIterable;
import com.basistheory.resources.agentic.allowances.credentials.AsyncCredentialsClient;
import com.basistheory.resources.agentic.allowances.rails.AsyncRailsClient;
import com.basistheory.resources.agentic.allowances.requests.AllowancesErrorsRequest;
import com.basistheory.resources.agentic.allowances.requests.AllowancesListRequest;
import com.basistheory.resources.agentic.allowances.requests.AllowancesUpdateRequest;
import com.basistheory.resources.agentic.allowances.requests.CreateAllowanceRequest;
import com.basistheory.types.Allowance;
import com.basistheory.types.AllowanceVerificationResponse;
import com.basistheory.types.SharedPaymentProviderError;
import com.basistheory.types.VerifyAllowanceRequest;
import java.util.concurrent.CompletableFuture;
import java.util.function.Supplier;

public class AsyncAllowancesClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawAllowancesClient rawClient;

    protected final Supplier<AsyncRailsClient> railsClient;

    protected final Supplier<AsyncCredentialsClient> credentialsClient;

    public AsyncAllowancesClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawAllowancesClient(clientOptions);
        this.railsClient = Suppliers.memoize(() -> new AsyncRailsClient(clientOptions));
        this.credentialsClient = Suppliers.memoize(() -> new AsyncCredentialsClient(clientOptions));
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawAllowancesClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Lists allowances for the current tenant. Defaults to active, unexpired resources; use <code>status=all</code> for a complete Portal history. Results can be scoped to one payment method, and server-side page filling prevents sparse pages when filtering by status.
     */
    public CompletableFuture<SyncPagingIterable<Allowance>> list() {
        return this.rawClient.list().thenApply(response -> response.body());
    }

    /**
     * Lists allowances for the current tenant. Defaults to active, unexpired resources; use <code>status=all</code> for a complete Portal history. Results can be scoped to one payment method, and server-side page filling prevents sparse pages when filtering by status.
     */
    public CompletableFuture<SyncPagingIterable<Allowance>> list(RequestOptions requestOptions) {
        return this.rawClient.list(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists allowances for the current tenant. Defaults to active, unexpired resources; use <code>status=all</code> for a complete Portal history. Results can be scoped to one payment method, and server-side page filling prevents sparse pages when filtering by status.
     */
    public CompletableFuture<SyncPagingIterable<Allowance>> list(AllowancesListRequest request) {
        return this.rawClient.list(request).thenApply(response -> response.body());
    }

    /**
     * Lists allowances for the current tenant. Defaults to active, unexpired resources; use <code>status=all</code> for a complete Portal history. Results can be scoped to one payment method, and server-side page filling prevents sparse pages when filtering by status.
     */
    public CompletableFuture<SyncPagingIterable<Allowance>> list(
            AllowancesListRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Create a spending allowance from a payment method. Supply <code>merchant</code> to scope the mandate to one merchant, or omit it to leave the allowance open and name a merchant on each credential request instead. The payment method must have at least one enabled rail; otherwise the request returns <code>NO_ACTIVE_RAILS</code>.
     */
    public CompletableFuture<Allowance> create(CreateAllowanceRequest request) {
        return this.rawClient.create(request).thenApply(response -> response.body());
    }

    /**
     * Create a spending allowance from a payment method. Supply <code>merchant</code> to scope the mandate to one merchant, or omit it to leave the allowance open and name a merchant on each credential request instead. The payment method must have at least one enabled rail; otherwise the request returns <code>NO_ACTIVE_RAILS</code>.
     */
    public CompletableFuture<Allowance> create(CreateAllowanceRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(request, requestOptions).thenApply(response -> response.body());
    }

    public CompletableFuture<Allowance> get(String allowanceId) {
        return this.rawClient.get(allowanceId).thenApply(response -> response.body());
    }

    public CompletableFuture<Allowance> get(String allowanceId, RequestOptions requestOptions) {
        return this.rawClient.get(allowanceId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Cancel an allowance so new credentials cannot be created from it. Network-side purchase instructions held by its rails are cancelled with the provider.
     */
    public CompletableFuture<Void> delete(String allowanceId) {
        return this.rawClient.delete(allowanceId).thenApply(response -> response.body());
    }

    /**
     * Cancel an allowance so new credentials cannot be created from it. Network-side purchase instructions held by its rails are cancelled with the provider.
     */
    public CompletableFuture<Void> delete(String allowanceId, RequestOptions requestOptions) {
        return this.rawClient.delete(allowanceId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Updates one or more mutable fields by changing the provider-side mandate first, then committing the same amount, prompt, and expiry locally. Mints are blocked while the update is in flight, and an empty request body is rejected.
     */
    public CompletableFuture<Allowance> update(String allowanceId) {
        return this.rawClient.update(allowanceId).thenApply(response -> response.body());
    }

    /**
     * Updates one or more mutable fields by changing the provider-side mandate first, then committing the same amount, prompt, and expiry locally. Mints are blocked while the update is in flight, and an empty request body is rejected.
     */
    public CompletableFuture<Allowance> update(String allowanceId, RequestOptions requestOptions) {
        return this.rawClient.update(allowanceId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Updates one or more mutable fields by changing the provider-side mandate first, then committing the same amount, prompt, and expiry locally. Mints are blocked while the update is in flight, and an empty request body is rejected.
     */
    public CompletableFuture<Allowance> update(String allowanceId, AllowancesUpdateRequest request) {
        return this.rawClient.update(allowanceId, request).thenApply(response -> response.body());
    }

    /**
     * Updates one or more mutable fields by changing the provider-side mandate first, then committing the same amount, prompt, and expiry locally. Mints are blocked while the update is in flight, and an empty request body is rejected.
     */
    public CompletableFuture<Allowance> update(
            String allowanceId, AllowancesUpdateRequest request, RequestOptions requestOptions) {
        return this.rawClient.update(allowanceId, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists sanitized provider failures for an allowance, including failed verification and credential attempts. Raw provider bodies and card data are never returned.
     */
    public CompletableFuture<SyncPagingIterable<SharedPaymentProviderError>> errors(String allowanceId) {
        return this.rawClient.errors(allowanceId).thenApply(response -> response.body());
    }

    /**
     * Lists sanitized provider failures for an allowance, including failed verification and credential attempts. Raw provider bodies and card data are never returned.
     */
    public CompletableFuture<SyncPagingIterable<SharedPaymentProviderError>> errors(
            String allowanceId, RequestOptions requestOptions) {
        return this.rawClient.errors(allowanceId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists sanitized provider failures for an allowance, including failed verification and credential attempts. Raw provider bodies and card data are never returned.
     */
    public CompletableFuture<SyncPagingIterable<SharedPaymentProviderError>> errors(
            String allowanceId, AllowancesErrorsRequest request) {
        return this.rawClient.errors(allowanceId, request).thenApply(response -> response.body());
    }

    /**
     * Lists sanitized provider failures for an allowance, including failed verification and credential attempts. Raw provider bodies and card data are never returned.
     */
    public CompletableFuture<SyncPagingIterable<SharedPaymentProviderError>> errors(
            String allowanceId, AllowancesErrorsRequest request, RequestOptions requestOptions) {
        return this.rawClient.errors(allowanceId, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Start or continue self-served verification for a rail that requires it. Public and private applications may call this operation with <code>agentic:allowance:verify</code>; browser clients should use a public application key. Visa verification is advanced through explicit ceremony actions. Mastercard managed authentication is finalized with <code>complete</code> after the hosted ceremony; callback delivery is only a browser signal and is not required.
     */
    public CompletableFuture<AllowanceVerificationResponse> verify(String allowanceId, VerifyAllowanceRequest request) {
        return this.rawClient.verify(allowanceId, request).thenApply(response -> response.body());
    }

    /**
     * Start or continue self-served verification for a rail that requires it. Public and private applications may call this operation with <code>agentic:allowance:verify</code>; browser clients should use a public application key. Visa verification is advanced through explicit ceremony actions. Mastercard managed authentication is finalized with <code>complete</code> after the hosted ceremony; callback delivery is only a browser signal and is not required.
     */
    public CompletableFuture<AllowanceVerificationResponse> verify(
            String allowanceId, VerifyAllowanceRequest request, RequestOptions requestOptions) {
        return this.rawClient.verify(allowanceId, request, requestOptions).thenApply(response -> response.body());
    }

    public AsyncRailsClient rails() {
        return this.railsClient.get();
    }

    public AsyncCredentialsClient credentials() {
        return this.credentialsClient.get();
    }
}
