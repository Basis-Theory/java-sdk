/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.basistheory.resources.notifications;

import com.basistheory.core.BasisTheoryApiApiException;
import com.basistheory.core.BasisTheoryApiHttpResponse;
import com.basistheory.core.BasisTheoryException;
import com.basistheory.core.ClientOptions;
import com.basistheory.core.MediaTypes;
import com.basistheory.core.ObjectMappers;
import com.basistheory.core.QueryStringMapper;
import com.basistheory.core.RequestOptions;
import com.basistheory.resources.notifications.requests.PostNotificationsMastercardCardRequest;
import com.basistheory.resources.notifications.requests.PostNotificationsVisaPanMetadataRequest;
import com.basistheory.resources.notifications.requests.PostNotificationsVisaProvisionedTokenRequest;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.util.HashMap;
import java.util.Map;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawNotificationsClient {
    protected final ClientOptions clientOptions;

    public RawNotificationsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Receives digital card status change events from Mastercard SRC. No API key authentication (relies on network-level security). Always returns 200 for Mastercard retry semantics.
     */
    public BasisTheoryApiHttpResponse<Void> mastercardDigitalCardUpdateNotification() {
        return mastercardDigitalCardUpdateNotification(
                PostNotificationsMastercardCardRequest.builder().build());
    }

    /**
     * Receives digital card status change events from Mastercard SRC. No API key authentication (relies on network-level security). Always returns 200 for Mastercard retry semantics.
     */
    public BasisTheoryApiHttpResponse<Void> mastercardDigitalCardUpdateNotification(
            PostNotificationsMastercardCardRequest request) {
        return mastercardDigitalCardUpdateNotification(request, null);
    }

    /**
     * Receives digital card status change events from Mastercard SRC. No API key authentication (relies on network-level security). Always returns 200 for Mastercard retry semantics.
     */
    public BasisTheoryApiHttpResponse<Void> mastercardDigitalCardUpdateNotification(
            PostNotificationsMastercardCardRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("notifications/mastercard/card")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new BasisTheoryException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new BasisTheoryApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new BasisTheoryApiApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new BasisTheoryException("Network error executing HTTP request", e);
        }
    }

    /**
     * Receives token lifecycle events from Visa Token Service. Authenticated via x-pay-token HMAC signature (not BT-API-KEY). Always returns 200 for Visa retry semantics.
     */
    public BasisTheoryApiHttpResponse<Void> visaTokenStatusUpdateNotification() {
        return visaTokenStatusUpdateNotification(
                PostNotificationsVisaProvisionedTokenRequest.builder().build());
    }

    /**
     * Receives token lifecycle events from Visa Token Service. Authenticated via x-pay-token HMAC signature (not BT-API-KEY). Always returns 200 for Visa retry semantics.
     */
    public BasisTheoryApiHttpResponse<Void> visaTokenStatusUpdateNotification(
            PostNotificationsVisaProvisionedTokenRequest request) {
        return visaTokenStatusUpdateNotification(request, null);
    }

    /**
     * Receives token lifecycle events from Visa Token Service. Authenticated via x-pay-token HMAC signature (not BT-API-KEY). Always returns 200 for Visa retry semantics.
     */
    public BasisTheoryApiHttpResponse<Void> visaTokenStatusUpdateNotification(
            PostNotificationsVisaProvisionedTokenRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("notifications/visa/provisionedToken");
        if (request.getApiKey().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "apiKey", request.getApiKey().get(), false);
        }
        if (request.getEventType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "eventType", request.getEventType().get(), false);
        }
        Map<String, Object> properties = new HashMap<>();
        if (request.getVProvisionedTokenId().isPresent()) {
            properties.put("vProvisionedTokenID", request.getVProvisionedTokenId());
        }
        if (request.getMessageType().isPresent()) {
            properties.put("messageType", request.getMessageType());
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(properties), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new BasisTheoryApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new BasisTheoryApiApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new BasisTheoryException("Network error executing HTTP request", e);
        }
    }

    /**
     * Receives PAN lifecycle events from Visa Token Service when card metadata changes (e.g. reissued card). Authenticated via x-pay-token HMAC signature (not BT-API-KEY). Always returns 200 for Visa retry semantics.
     */
    public BasisTheoryApiHttpResponse<Void> visaPanMetadataUpdateNotification() {
        return visaPanMetadataUpdateNotification(
                PostNotificationsVisaPanMetadataRequest.builder().build());
    }

    /**
     * Receives PAN lifecycle events from Visa Token Service when card metadata changes (e.g. reissued card). Authenticated via x-pay-token HMAC signature (not BT-API-KEY). Always returns 200 for Visa retry semantics.
     */
    public BasisTheoryApiHttpResponse<Void> visaPanMetadataUpdateNotification(
            PostNotificationsVisaPanMetadataRequest request) {
        return visaPanMetadataUpdateNotification(request, null);
    }

    /**
     * Receives PAN lifecycle events from Visa Token Service when card metadata changes (e.g. reissued card). Authenticated via x-pay-token HMAC signature (not BT-API-KEY). Always returns 200 for Visa retry semantics.
     */
    public BasisTheoryApiHttpResponse<Void> visaPanMetadataUpdateNotification(
            PostNotificationsVisaPanMetadataRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("notifications/visa/panMetadata");
        if (request.getApiKey().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "apiKey", request.getApiKey().get(), false);
        }
        Map<String, Object> properties = new HashMap<>();
        if (request.getVPanEnrollmentId().isPresent()) {
            properties.put("vPanEnrollmentID", request.getVPanEnrollmentId());
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(properties), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new BasisTheoryApiHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new BasisTheoryApiApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new BasisTheoryException("Network error executing HTTP request", e);
        }
    }
}
